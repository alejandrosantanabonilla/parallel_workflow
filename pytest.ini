# This file explicitly configures pytest for your project.

[pytest]
# Register the custom 'mpi' marker to prevent tests from being skipped.
markers =
    mpi: marks tests as MPI-dependent and requiring parallel execution
